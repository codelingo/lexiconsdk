#!/usr/bin/env bash

# cd "$(dirname "$0")"

# sudo cp -R testdata/ /testdata/
# src/build.sh
# sudo cp -R src/ /server/
# sudo cp server-config.yaml /server/
# sudo mkdir -p /setup/
# sudo cp setup.json /setup/

# user="$(whoami)"
# sudo chown -R $user:$user /testdata /server /setup

